import { FC } from 'react'
import { Surface } from '@react-canvas-ui/core'

export const Bunnymark: FC = () => {
  return (
    <div><Surface /></div>
  )
}

export default {
  title: 'renderer/ParticleContainer',
}
